name: Prepare Release

on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Release type (patch, minor, major)'
        required: true
        default: 'patch'
        type: choice
        options: [patch, minor, major]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          # Using 3.14 to match your publish.yml environment
          python-version: "3.14"

      - name: Install Hatch
        run: pip install hatch

      - name: Bump version and push
        id: versioning
        env:
          # Use the App token for the push command
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          hatch version ${{ github.event.inputs.bump_type }}
          NEW_VERSION=$(hatch version)
          echo "new_tag=v$NEW_VERSION" >> $GITHUB_OUTPUT
          
          git add -A
          git commit -m "chore: release v$NEW_VERSION"
          
          # Explicitly use the token in the push URL for maximum reliability
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git main

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh release create ${{ steps.versioning.outputs.new_tag }} \
            --target main \
            --title "${{ steps.versioning.outputs.new_tag }}" \
            --generate-notes
